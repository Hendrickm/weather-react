import React from 'react';
import './navbar.css';
import {
  Navbar, Form, FormControl, Button,
} from 'react-bootstrap';

export default () => (
  <Navbar expand="xl">
    <Navbar.Brand href="#home">Weather React</Navbar.Brand>
  </Navbar>
);
